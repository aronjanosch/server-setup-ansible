# group_vars/all/secret_template.yml
# Lines starting with '#' (comments) are ignored by the script.

user_password: "PLACEHOLDER_USER_PASSWORD"

# Add as many placeholders as you need:
# api_key: "PLACEHOLDER_API_KEY"